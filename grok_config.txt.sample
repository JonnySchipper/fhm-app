YOUR-GROK-API-KEY-HERE

Instructions:
1. Get your Grok API key from https://console.x.ai/
2. Create a file called "grok_config.txt" in the PARENT folder (one level up)
3. Put your API key as the only content (one line)
4. Save the file

Example location:
If this is at: c:\dev\canva\rafas_folder\
Create key at: c:\dev\canva\grok_config.txt

Alternative: Place "grok_config.txt" in this folder (rafas_folder)

Note: grok_config.txt is in .gitignore and won't be pushed to GitHub

